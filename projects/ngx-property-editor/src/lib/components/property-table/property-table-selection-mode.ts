/**
 * Possible row selection modes of the `PropertyTableComponent`.
 * @see PropertyTableComponent.selectionMode
 */
export type PropertyTableSelectionMode =
  /** Disable selection of rows. */
  'none' |
  /** Select a single row. */
  'single' |
  /** Select multiple rows. */
  'multiple';
